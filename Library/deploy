#!/bin/bash
# Performs some post-build steps.
# $1 is expected to be set to the build output directory to get the
# files to deploy from.

source "$(dirname "$0")/../Common/deploy"

chmod u+x $deploymentDir/testconsole
